const pets = ['cat', 'dog', 'bat'];

//arroja un false o true en el caso de que no encuentre o si encuentre el objeto, funciona con arrays y strings

const rta = pets.includes('dog');
console.log('includes', rta)